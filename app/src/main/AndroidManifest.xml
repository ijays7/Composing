<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ijays.composeit">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ComposeIt">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.ComposeIt.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".text.TextDisplayActivity" />

        <activity android:name=".text.TextStyleActivity" />

        <activity android:name=".text.TextFieldActivity" />

        <activity android:name=".image.ImageDisplayActivity" />

        <activity android:name=".button.MaterialButtonActivity" />

        <activity android:name=".dialog.DialogTestActivity" />

        <activity android:name=".state.livedata.LiveDataActivity" />

        <activity android:name=".anim.AnimActivity" />

        <activity android:name=".layout.LayoutCodeLabTestActivity" />

        <activity android:name=".basics.BasicActivity" />
    </application>

</manifest>